# IAM Module

Manages IAM roles, policies, and instance profiles for EC2, ECS, and Lambda services.

Each resource type can be independently toggled via `create_*` variables. Roles follow the naming convention `${project}-${environment}-{service}-{resource}`.

<!-- BEGIN_TF_DOCS -->
<!-- END_TF_DOCS -->
